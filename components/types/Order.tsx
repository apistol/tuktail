export interface OrderItem {
    id: number;
    name: string;
    qty: number

    // TODO this needs refactor and replace menuItems
    // title
    // description
    // imageSrc
    // nextId
    // menuName
}
